import { OrderNodePipe } from './order-node.pipe';

describe('OrderNodePipe', () => {
    it('create an instance', () => {
        const pipe = new OrderNodePipe();
        expect(pipe).toBeTruthy();
    });
});
